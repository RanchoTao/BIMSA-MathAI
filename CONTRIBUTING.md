# 贡献指南

感谢你参与 BIMSA MathAI Wiki。请优先提交小而清晰、来源可追溯的改动；不要为了整理而批量移动历史资料。

## 新增或修改页面

1. 在 `docs/` 下选择现有分类，以小写英文短横线命名目录与 `.md` 文件，例如 `docs/tools/git-basics.md`。
2. 页面第一行使用清晰的一级标题；经验性内容应注明适用时间、对象和前提，事实信息应给出来源。
3. 使用相对链接连接 `docs/` 内页面。大附件不要复制进 `docs/`，应链接 GitHub 中的原文件。
4. 仅当页面是主要阅读路径时才加入 `mkdocs.yml`；不要机械地把每个文件放入侧栏。
5. 新建课程主页可复制 `templates/course-page.md`，删去占位说明后填写。

## 命名与 Markdown 规范

- 新网页路径使用小写 ASCII 字母、数字和短横线，不使用空格、中文标点或日期式随意编号，以保持 URL 稳定。
- 历史目录中的中文路径暂不批量重命名；需要整理时先建立索引并在 Pull Request 中说明映射。
- 标题按层级递进，不跳级；代码块标注语言；表格保持简洁并提供表头。
- 数学公式使用 `\(...\)` 和 `\[...\]`。提示内容可使用 Material 的 `!!! note`、`!!! warning` 等语法。
- 内部链接必须在本地严格构建中通过；外部资源注明名称、来源与用途。

## 图片、PDF 与其他附件

- 网页插图放在 `docs/assets/images/` 下按专题建子目录，使用描述性英文文件名，并尽可能压缩；正文必须提供有意义的替代文本。
- PDF、PPT、Word、Notebook、代码和压缩包不应批量加入网站导航，也不要直接嵌入大文件。
- 已有历史附件保留原位，通过课程或学期资料索引链接；GitHub Pages 不便预览的格式应标明类型并链接 GitHub 原文件。
- 新附件必须确认来源、授权和传播范围。能以 Markdown 表达的原创内容优先写成网页正文。

## 本地预览与检查

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
mkdocs serve
```

提交前至少运行：

```bash
mkdocs build --strict
git status --short
```

不要提交生成的 `site/`、虚拟环境、缓存或编辑器临时文件。

## 合规底线

- 禁止上传个人隐私、联系方式、学号、成绩、签名或其他可识别信息；作业、考试与截图必须完成脱敏。
- 禁止上传受限传播的课程平台内容、保密材料和无权再分发的版权材料。
- 不要提交答案型作弊材料，包括可直接用于尚在进行的作业、考试或考核的完整答案。
- 分享复盘时侧重思路、常见错误与学习方法，并遵守课程规则和学术诚信要求。
- 若无法确认是否有权公开，请先不上传，可在 Issue 中只描述待确认事项。

## Pull Request

请说明改动目标、涉及页面、资料来源、是否调整导航、是否涉及历史资料，以及执行过的检查。结构调整同步更新 `CHANGELOG.md`；大规模迁移应另开提案讨论。
